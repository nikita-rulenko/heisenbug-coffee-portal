data/
bin/
*.db
.env
.DS_Store
server

# Scratch test files written by the heisenbug-research test-writing benchmark.
# These are generated by an LLM, run once via `go test`, then deleted.
# If this pattern catches anything in your tree, your bench harness crashed
# mid-run — sweep with `make scratch-clean` from the bench dir.
zz_bench_*_test.go
